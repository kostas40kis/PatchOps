You are using PatchOps to start work on a brand-new target project.

Before you touch any target-specific assumptions, read this generic PatchOps packet first:

1. `README.md`
2. `docs/overview.md`
3. `docs/llm_usage.md`
4. `docs/manifest_schema.md`
5. `docs/profile_system.md`
6. `docs/compatibility_notes.md`
7. `docs/failure_repair_guide.md`
8. `docs/examples.md`
9. `docs/project_status.md`
10. `docs/operator_quickstart.md`
11. `docs/project_packet_contract.md`
12. `docs/project_packet_workflow.md`

After reading those files:
1. restate what PatchOps is and is not,
2. state which profile is most likely correct for the target,
3. state what must remain outside PatchOps,
4. create or update `docs/projects/<project_name>.md` using the project-packet template,
5. choose the nearest bundled example manifest instead of inventing one from scratch,
6. run the safe review sequence: `check`, `inspect`, `plan`,
7. use `apply` or `verify`,
8. inspect the canonical report,
9. update the project packet after validated progress.

Architecture rules you must preserve:
- PatchOps stays project-agnostic.
- Profiles remain the executable target abstraction.
- Project packets are the human-readable target contract.
- Manifests tell PatchOps what to do now.
- Reports prove what happened.
- Handoff files are for continuation of already-running work, not first-time onboarding.
- PowerShell stays thin.
- Reusable workflow logic stays in Python.
- One canonical report per run remains mandatory.

Do not move target business logic into generic PatchOps code.
Do not redesign the wrapper unless current evidence proves a missing surface.

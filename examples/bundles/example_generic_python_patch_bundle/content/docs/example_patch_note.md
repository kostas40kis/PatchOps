# Example Patch Note

This file exists only to give the maintained example bundle one simple doc target.

The important teaching points are:
- the bundle uses one root-level launcher,
- the delivered zip should contain the bundle root contents directly,
- the manual unzip stage still ends with one canonical Desktop txt report.

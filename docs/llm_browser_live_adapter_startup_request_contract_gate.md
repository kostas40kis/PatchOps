# L1.8d Startup request contract gate check-name repair

This repair restores the expected L1.8 contract-gate check names and operator heading while preserving the passive boundary.

Expected PASS means startup remains blocked, no browser/session is created, no Selenium or browser optional dependency is imported, and requested side effects are only modelled in JSON payloads.

Next patch: L1.9 Live adapter startup request fixture matrix.

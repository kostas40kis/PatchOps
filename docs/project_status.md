# Project Status

## Posture

PatchOps is in a maintenance / additive-improvement posture.

## Current truth

The repo is post zip-first, post recovery-stream, and documentation has been refreshed from shipped behavior.

The current accepted posture includes:

- one canonical truth rule,
- thin PowerShell / Python-owned reusable mechanics,
- bundle-authoring preflight,
- suspicious-success blocking,
- future-LLM source-bundle truth surfaces,
- and self-hosted maintenance proof expectations.

## Development process summary

The maintained development process is:

1. implement the smallest truthful code patch,
2. prove it with the canonical report,
3. classify the first failing layer honestly,
4. repair narrowly,
5. refresh docs only after the code-proof frontier is green,
6. add documentation contract tests last.

## Notes

The accepted-frontier explanation should distinguish historical completion language from the currently accepted live repo state when needed, while keeping the current command inventory and report semantics explicit.

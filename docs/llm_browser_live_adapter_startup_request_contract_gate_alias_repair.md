# L1.8f startup request contract gate alias repair

L1.8f restores the remaining public check-name aliases for the passive startup
request contract gate.

The gate remains passive: no Selenium import, no browser start, no click,
download, paste, send, package-run, commit, or push.
